describe("Register User", () => {
    test.todo("rejects a request with invalid or missing fields")
    test.todo("rejects a request with a weak password")
    test.todo("rejects a request with an already existing e-mail")
    test.todo("rejects a request with an already existing username")
    test.todo("creates and returns a new user on success")
    test.todo("returns a valid JWT token on success")
})